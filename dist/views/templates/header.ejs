<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">

  <title><%= title %> | MyApp</title>
  <meta name="description" content="Proyecto con Express, MongoDB Atlas y Swagger">
  <meta name="author" content="Henry Arévalo">

  <!-- Favicon -->
  <link rel="icon" type="image/png" href="/favicon.png">

  <!-- Bootstrap CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

  <!-- Estilos propios -->
  <link rel="stylesheet" href="/css/styles.css">
</head>

<body>
  <!-- Navbar -->
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm fixed-top">
    <div class="container-fluid">
      <a class="navbar-brand fw-bold" href="/">
        <i class="bi bi-code-slash"></i> MyApp
      </a>

      <!-- Botón responsive -->
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarMenu">
        <span class="navbar-toggler-icon"></span>
      </button>

      <!-- Menú colapsable -->
      <div class="collapse navbar-collapse" id="navbarMenu">
        <ul class="navbar-nav ms-auto mb-2 mb-lg-0 align-items-center">
          <li class="nav-item">
            <a href="/api-docs" class="nav-link text-white" title="Swagger Docs">
              <i class="bi bi-journal-code fs-5"></i> Swagger Docs
            </a>
          </li>
          <li class="nav-item">
            <a href="/productos" class="nav-link text-white" title="Productos">
              <i class="bi bi-box-seam fs-5"></i> Productos
            </a>
          </li>
          <li class="nav-item">
            <a id="perfilLink" href="/perfil" class="nav-link text-white" title="Mi perfil">
              <i class="bi bi-person-circle fs-5"></i> Perfil
            </a>
          </li>
          <li class="nav-item">
            <a href="/" class="nav-link text-white" title="Inicio">
              <i class="bi bi-house-door fs-5"></i> Inicio
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- 🔥 Script para manejar sesión -->
 <script>
  document.addEventListener("DOMContentLoaded", () => {
    const token = localStorage.getItem("token");

    if (token) {
      try {
        // Decodifica el payload del JWT
        const payload = JSON.parse(atob(token.split(".")[1]));
        const exp = payload.exp * 1000; // convierte a milisegundos
        const tiempoRestante = exp - Date.now();

        if (tiempoRestante <= 0) {
          // Token ya expiró
          localStorage.removeItem("token");
          alert("🔒 Tu sesión se cerró por seguridad. Inicia sesión nuevamente.");
          window.location.href = "/login";
        } else {
          // Configura un temporizador para que avise cuando expire
          setTimeout(() => {
            localStorage.removeItem("token");
            alert("🔒 Tu sesión se cerró por seguridad. Inicia sesión nuevamente.");
            window.location.href = "/login";
          }, tiempoRestante);
        }
      } catch (err) {
        console.error("Token inválido o dañado", err);
        localStorage.removeItem("token");
        window.location.href = "/login";
      }
    }
  });
</script>


  <!-- Espaciado para el contenido -->
  <div style="margin-top: 70px;"></div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
